const name2 = "Freddy";
typeof name2 === "string";

if (!("serviceWorker" in navigator)) {
  // you have an old browser :-(
}

const greeting = "hello";
console.log(`${greeting} world!`);

[(1, 2, 3)].forEach(x => console.log(x));

// get all your prettier settings easily here
// https://prettier.io/playground/
